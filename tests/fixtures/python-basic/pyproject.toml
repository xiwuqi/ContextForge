[project]
name = "python-basic"
version = "0.1.0"
description = "Fixture python repository"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
